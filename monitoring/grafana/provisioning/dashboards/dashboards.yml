apiVersion: 1

providers:
  - name: Adjacent
    orgId: 1
    folder: Adjacent
    folderUid: adjacent
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
